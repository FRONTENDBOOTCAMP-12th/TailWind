import { css } from 'lit';

export default css`
    .product-check-container {
        display: flex;
        gap: 0.5rem;
        justify-content: center;
        font-size: var(--label---medium);
        font-weight: 600;

        button {
            cursor: pointer;
        }
    }
`;
